//find if given no is +ve or -ve
#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	if(n>0)
	printf("it is positive");
	if(n<0)
	printf("it is negative");
	if(n==0)
	printf("it is zero");
	return 0;
}

